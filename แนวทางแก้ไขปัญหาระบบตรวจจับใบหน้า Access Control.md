# แนวทางแก้ไขปัญหาระบบตรวจจับใบหน้า Access Control

จากการวิเคราะห์ไฟล์ `PROBLEM_ANALYSIS.txt` ซึ่งระบุปัญหาของระบบตรวจจับใบหน้า Access Control ที่ใช้กล้อง 4 ตัวร่วมกับโมเดล YOLOv8n พบว่าระบบมีปัญหาหลักในด้านประสิทธิภาพการทำงานพร้อมกัน (Concurrency), ความแม่นยำในการติดตาม (Tracking), การจัดการสถานะและทิศทาง (Direction/State), และการแสดงผลบน Web UI ที่ไม่สอดคล้องกับเหตุการณ์จริง

เพื่อแก้ไขปัญหาเหล่านี้อย่างเป็นระบบและมีประสิทธิภาพ ขอเสนอแนวทางแก้ไขโดยแบ่งตามลำดับความสำคัญ (Priority) ดังนี้ครับ

## 1. การแก้ไขปัญหาเร่งด่วน (Priority 1: Core Detection & Performance)

ปัญหาในกลุ่มนี้ส่งผลกระทบโดยตรงต่อความสามารถในการตรวจจับของระบบ ทำให้เกิด False Negative สูง และระบบทำงานล่าช้า

### 1.1 แก้ไขปัญหาคอขวดของ Model Inference (PROBLEM 1)
**ปัญหา:** ปัจจุบันกล้องทั้ง 4 ตัวใช้โมเดล YOLOv8n ตัวเดียวกันและถูกล็อกด้วย `model_infer_lock` ทำให้ไม่สามารถทำงานพร้อมกันได้ กล้องต้องรอคิวกันประมวลผล ส่งผลให้เฟรมเรตตกและพลาดการตรวจจับ
**แนวทางแก้ไข:**
- **แยก Instance ของโมเดล:** สร้างโมเดล YOLO แยกกันสำหรับกล้องที่ทำหน้าที่ต่างกัน เช่น `model_yolo` สำหรับกล้อง 4 (YOLO cam ที่ใช้ `track()`) และ `model_surv` สำหรับกล้อง 1, 2, 3 (Surveillance cams ที่ใช้ `model()`)
- **ลดการใช้ Lock ที่ไม่จำเป็น:** เมื่อแยกโมเดลแล้ว สามารถลดขอบเขตการใช้ `model_infer_lock` หรือยกเลิกไปเลยสำหรับโมเดลที่รันบน Thread แยกกันอย่างอิสระ (ต้องตรวจสอบว่า PyTorch/Ultralytics รองรับ Thread-safe inference ในสภาพแวดล้อมปัจจุบันหรือไม่ หากใช้ CPU อาจต้องระวังเรื่อง Thread contention แต่ถ้าใช้ GPU การแยกโมเดลจะช่วยได้มาก)
- **ไฟล์ที่ต้องแก้:** `app.py` บรรทัดที่ 95-110 (Global vars), 338-339, 885-886

### 1.2 ยกเลิกหรือปรับลด Rate Limit ของ Face Recognition (PROBLEM 3)
**ปัญหา:** กล้อง 2 มีการหน่วงเวลา (Rate Limit) ในการจดจำใบหน้าถึง 3 วินาที (`SURV_FACE_INTERVAL = 3.0`) ทำให้เมื่อคนเดินผ่านกล้อง ระบบอาจจับภาพใบหน้าที่ชัดที่สุดไม่ทัน หรือคนเดินพ้นเฟรมไปก่อน
**แนวทางแก้ไข:**
- **ลด Interval:** ปรับลด `SURV_FACE_INTERVAL` ของกล้อง 2 ลงเหลือ 0.5 - 1.0 วินาที หรือยกเลิกไปเลยหากทรัพยากรเครื่องเพียงพอ เพื่อให้ระบบสามารถจับภาพใบหน้าและจดจำได้ทันทีที่คนเดินเข้ามาในเฟรม
- **ปรับ Logic การแจ้งเตือน Unknown:** ปรับลด `UNKNOWN_DEFER_SEC` (ปัจจุบัน 20 วินาที) ให้สั้นลง เพื่อให้ระบบแจ้งเตือนบุคคลแปลกหน้าได้รวดเร็วขึ้น
- **ไฟล์ที่ต้องแก้:** `app.py` บรรทัดที่ 928-931, 1297-1312

## 2. การแก้ไขปัญหาการแสดงผลและสถานะ (Priority 2: Web UI & Direction)

ปัญหาในกลุ่มนี้ทำให้ผู้ใช้สับสน เนื่องจากระบบไม่สามารถแยกแยะทิศทางการเข้า-ออกได้ชัดเจน และ Web UI แสดงภาพผิดกล้อง

### 2.1 เพิ่มการจัดการทิศทาง (Direction) ของคนเข้า/ออก (PROBLEM 4)
**ปัญหา:** ระบบบันทึกเหตุการณ์ ENTER/EXIT ได้ แต่ไม่มีการแยกภาพสำหรับแต่ละเหตุการณ์ และการแจ้งเตือน Unknown ไม่ระบุทิศทาง
**แนวทางแก้ไข:**
- **แยกตัวแปรเก็บภาพ:** สร้างตัวแปรแยกสำหรับเก็บภาพตอนเข้าและออก เช่น `entry_capture_path` (ภาพจากกล้อง 2 ตอน ENTER) และ `exit_capture_path` (ภาพจากกล้อง 4 ตอน EXIT)
- **ปรับปรุง State Machine:** ให้ State Machine ส่งข้อมูลทิศทาง (Direction) ไปพร้อมกับ Event และบันทึกภาพให้ตรงกับประเภทของ Event
- **ไฟล์ที่ต้องแก้:** `app.py` บรรทัดที่ 1006-1017, 1237-1261, 1333-1350

### 2.2 แก้ไข Web UI ให้แสดงภาพตรงกับเหตุการณ์ (PROBLEM 5)
**ปัญหา:** Web UI ในส่วน "最新偵測成員" แสดงภาพจากกล้อง 1 (ประตูเข้า) เสมอ ไม่ว่าจะเกิดเหตุการณ์อะไร และฟังก์ชัน `_save_cam2_capture()` บันทึกภาพผิดกล้อง
**แนวทางแก้ไข:**
- **แก้ไขฟังก์ชันบันทึกภาพ:** แก้ไข `_save_cam2_capture()` ให้บันทึกภาพจากกล้อง 2 จริงๆ (เปลี่ยนเงื่อนไข `if cam_id == 1` เป็น `if cam_id == 2`) หรือเขียนฟังก์ชันใหม่ให้ชัดเจน เช่น `save_entry_capture()` และ `save_exit_capture()`
- **ปรับปรุง API:** แก้ไข `get_latest_face()` ให้คืนค่า URL ของภาพที่สอดคล้องกับ Event ล่าสุด (ENTER คืนค่าภาพจากกล้อง 2, EXIT คืนค่าภาพจากกล้อง 4)
- **อัปเดต Frontend:** แก้ไข `index.html` ให้รองรับการแสดงผลภาพตามประเภทของ Event
- **ไฟล์ที่ต้องแก้:** `app.py` บรรทัดที่ 832-842, 1728-1764, `index.html` บรรทัดที่ 471-488, 808-867

## 3. การปรับปรุงประสิทธิภาพและความเสถียร (Priority 3: Optimization & Tracking)

ปัญหาในกลุ่มนี้ส่งผลต่อความเสถียรในระยะยาวและการใช้ทรัพยากรระบบ

### 3.1 ปรับปรุงความเสถียรของการ Tracking กล้อง 2 (PROBLEM 2)
**ปัญหา:** กล้อง 2 ใช้ Custom Tracking (`_match_boxes()`) ที่พึ่งพาแค่ IoU และระยะห่างจุดศูนย์กลาง ทำให้ Track หลุดง่ายเมื่อคนขยับเร็ว ส่งผลให้ต้องเริ่ม Recognize ใหม่
**แนวทางแก้ไข:**
- **เปลี่ยนไปใช้ Built-in Tracker:** หากเป็นไปได้ ให้เปลี่ยนกล้อง 2 ไปใช้ `model.track(persist=True)` เหมือนกล้อง 4 เพื่อใช้ประโยชน์จาก Tracker ที่มีประสิทธิภาพดีกว่า (เช่น BoT-SORT หรือ ByteTrack)
- **ปรับปรุง Custom Tracker:** หากไม่สามารถใช้ Built-in Tracker ได้ ให้เพิ่มอัลกอริทึมคาดการณ์การเคลื่อนไหว (Motion Prediction) เช่น Kalman Filter เข้าไปใน `_match_boxes()` เพื่อให้ Tracking เสถียรขึ้น
- **ไฟล์ที่ต้องแก้:** `app.py` บรรทัดที่ 587-647, 857-861

### 3.2 แชร์ Identity ข้ามกล้อง (PROBLEM 6)
**ปัญหา:** แต่ละกล้องทำการ Face Recognition แยกกัน ทำให้สิ้นเปลือง CPU และอาจได้ผลลัพธ์ที่ไม่ตรงกัน
**แนวทางแก้ไข:**
- **สร้าง Global Identity Pool:** สร้างระบบแชร์ข้อมูล Identity กลาง (เช่น Dictionary ที่เก็บ `track_id` หรือ `face_encoding` ที่แมปกับชื่อคน)
- **ลดการประมวลผลซ้ำซ้อน:** เมื่อกล้องใดกล้องหนึ่ง (เช่น กล้อง 2 ที่เห็นหน้าชัดสุด) สามารถ Identify บุคคลได้แล้ว ให้แชร์ข้อมูลนี้ไปยังกล้องอื่นที่กำลัง Track บุคคลเดียวกันอยู่ (อาจต้องมีการทำ Camera Calibration หรือใช้เทคนิค Re-ID เพื่อแมปบุคคลข้ามกล้อง)
- **ไฟล์ที่ต้องแก้:** `app.py` บรรทัดที่ 706-830, 857-861

---

## สรุปแผนการดำเนินการ (Action Plan)

เพื่อให้การแก้ไขเป็นไปอย่างราบรื่น แนะนำให้ดำเนินการตามลำดับดังนี้:

1. **Phase 1 (P1):** แก้ไข `app.py` เพื่อแยก Instance ของโมเดล YOLO และลด Rate Limit ของกล้อง 2 ทดสอบรันระบบเพื่อดูว่าอาการคอขวดหายไปหรือไม่
2. **Phase 2 (P2):** แก้ไขตัวแปรเก็บภาพใน `app.py` ให้แยก Entry/Exit ชัดเจน และอัปเดต `index.html` ให้แสดงผลถูกต้อง ทดสอบการเดินเข้า-ออกเพื่อดูการแสดงผลบน Web UI
3. **Phase 3 (P3):** ปรับปรุงระบบ Tracking ของกล้อง 2 และพิจารณาทำระบบแชร์ Identity ข้ามกล้องเพื่อลดภาระ CPU


